<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhta.mybatis.StoresMapper">
	<select id="myPositionList" parameterType="hashmap" resultType="stores">
		select s.*
		from stores s, category c
		where s.cat_num=c.cat_num and c.cat_num=#{cat_num} 
		and s.sto_num in(select sto_num from able_location where able_loc=#{able_loc})
	</select>
</mapper>